[build]
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# 将 /api/sync 重定向到 Netlify Function
[[redirects]]
  from = "/api/sync"
  to = "/.netlify/functions/sync"
  status = 200
  force = true
